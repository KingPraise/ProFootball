{
  "name": "profootball-realtime-backend",
  "version": "1.0.0",
  "description": "Real-time football match center backend with REST API, WebSocket, Supabase, and Redis integration.",
  "main": "src/api/server.js",
  "scripts": {
    "dev": "nodemon src/api/server.js",
    "start": "node src/api/server.js",
    "build": "echo 'No build step (JS only)'",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@supabase/supabase-js": "2.39.8",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "ioredis": "^5.3.2",
    "socket.io": "^4.7.5",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "globals": "^17.0.0",
    "nodemon": "^3.0.3",
    "prettier": "^3.2.5",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "socket.io-client": "^4.7.5"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "scripts": {
    "dev": "nodemon src/api/server.js",
    "start": "node src/api/server.js",
    "build": "echo 'No build step (JS only)'",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "jest"
  }
}
